appId: com.ducatprotocol.DucatProtocolWallet
---
# Send Half Amount - Verify HALF preset button works on send screen
# Funded wallet → send BTC → address → HALF → verify amount updates

- runFlow: ../helpers/import-wallet-typed.yaml

- extendedWaitUntil:
    visible:
      id: "wallet-screen"
    timeout: 15000
- runFlow: ../helpers/dismiss-low-ecash.yaml
- runFlow: ../helpers/dismiss-airdrop.yaml

- runFlow:
    when:
      visible:
        id: "passkey-migration-modal"
    commands:
      - tapOn:
          id: "passkey-skip-btn"

# Wait for wallet actions
- extendedWaitUntil:
    visible:
      id: "wallet-actions"
    timeout: 15000

# Open send BTC
- tapOn:
    id: "wallet-withdraw-btn"
- extendedWaitUntil:
    visible:
      id: "withdraw-btc"
    timeout: 5000
- tapOn:
    id: "withdraw-btc"
- waitForAnimationToEnd
- extendedWaitUntil:
    visible:
      id: "send-input-screen"
    timeout: 10000

# Enter address
- extendedWaitUntil:
    visible:
      text: "Recipient Bitcoin address.*"
    timeout: 5000
- tapOn:
    text: "Recipient Bitcoin address.*"
- inputText: "tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx"
- hideKeyboard
- extendedWaitUntil:
    visible:
      text: "Valid"
    timeout: 5000

# Tap HALF preset
- tapOn:
    text: "HALF"
- waitForAnimationToEnd

# Verify Send BTC is still shown (screen didn't change)
- assertVisible:
    text: "Send BTC"
- assertVisible:
    id: "send-input-screen"
