appId: com.ducatprotocol.DucatProtocolWallet
---
# Vault - Verify Create Vault button visible on new wallet
# Import wallet → verify create vault button by accessibility label

- runFlow: ../../helpers/import-wallet.yaml

# Verify wallet screen
- assertVisible:
    id: "wallet-screen"

# Verify vault card section exists (accessibility label on the disabled card)
- assertVisible:
    id: "vault-overlay"
