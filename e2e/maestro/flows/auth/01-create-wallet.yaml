appId: com.ducatprotocol.DucatProtocolWallet
---
# Create Wallet - Passkey PIN creation flow (E2E bypasses native passkey dialog)
# clearState → welcome → passkey PIN (enter + confirm) → wallet

- runFlow: ../helpers/clear-and-launch.yaml

# Tap create button — first tap may dismiss Expo Dev Client overlay
- tapOn:
    id: "welcome-create-wallet-btn"

# If overlay intercepted first tap, we're still on welcome screen — retry
- runFlow:
    when:
      visible:
        id: "welcome-screen"
    commands:
      - tapOn:
          id: "welcome-create-wallet-btn"

# PasskeyPinInput screen — enter 6-digit PIN
- extendedWaitUntil:
    visible:
      id: "pin-setup-screen"
    timeout: 30000

# Enter PIN (123456)
- runFlow: ../helpers/enter-pin-setup.yaml

# Confirm PIN (123456 again)
- runFlow: ../helpers/enter-pin-setup.yaml

# Skip biometric if prompted
- runFlow:
    when:
      visible:
        id: "biometric-modal"
    commands:
      - tapOn:
          id: "biometric-skip-btn"

# Skip passkey migration if prompted
- runFlow:
    when:
      visible:
        id: "passkey-migration-modal"
    commands:
      - tapOn:
          id: "passkey-skip-btn"

# Airdrop is not mandatory for auth coverage. Slow network/faucet responses
# should not fail the wallet creation smoke test.
- waitForAnimationToEnd
- waitForAnimationToEnd
- runFlow: ../helpers/unlock-if-locked.yaml

# Passkey prompt can appear just after wallet state is committed.
- runFlow:
    when:
      visible:
        id: "passkey-migration-modal"
    commands:
      - tapOn:
          id: "passkey-skip-btn"

# Dismiss airdrop modal if it appears
- runFlow: ../helpers/dismiss-airdrop.yaml
- runFlow: ../helpers/unlock-if-locked.yaml

- runFlow:
    when:
      visible:
        id: "passkey-migration-modal"
    commands:
      - tapOn:
          id: "passkey-skip-btn"

# Dismiss low ecash modal if it appears
- runFlow: ../helpers/dismiss-low-ecash.yaml

# Verify wallet screen loads (may take time after PIN setup)
- extendedWaitUntil:
    visible:
      id: "wallet-screen"
    timeout: 15000
