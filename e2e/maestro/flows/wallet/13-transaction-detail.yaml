appId: com.ducatprotocol.DucatProtocolWallet
---
# Transaction Detail - Verify history shows transactions (not empty state)
# Create wallet (airdrop tx) → open history → verify non-empty transaction list

- runFlow: ../helpers/create-wallet.yaml

# Wait for animations
- waitForAnimationToEnd
- waitForAnimationToEnd

# Dismiss modals that may appear
- runFlow: ../helpers/dismiss-low-ecash.yaml
- runFlow: ../helpers/dismiss-airdrop.yaml
- runFlow: ../helpers/dismiss-passkey-recovery.yaml

# Navigate to transaction history (opens as bottom sheet)
- tapOn:
    id: "wallet-history-btn"

# Wait for Transaction History bottom sheet title
- waitForAnimationToEnd
- extendedWaitUntil:
    visible:
      text: "Transaction History"
    timeout: 10000

# Verify a transaction row appears. Live explorer/indexer reads can lag or
# briefly fail, so wait for the concrete first-row testID instead of sampling
# the empty state once.
- extendedWaitUntil:
    visible:
      id: "transaction-history-row-0"
    timeout: 180000
