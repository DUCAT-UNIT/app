appId: com.ducatprotocol.DucatProtocolWallet
---
# Unlock PIN lock screen if it appears
# Uses PIN: 111111 (same as setup)
- runFlow:
    when:
      visible:
        text: "Enter PIN"
    commands:
      - runFlow: enter-pin-lock.yaml
