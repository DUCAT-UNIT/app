appId: com.ducatprotocol.DucatProtocolWallet
---
# Dismiss "Low Turbo UNIT Balance" modal if it appears
- runFlow:
    when:
      visible:
        id: "low-ecash-modal"
    commands:
      - tapOn:
          id: "low-ecash-dismiss-btn"
      - waitForAnimationToEnd
